{use /std/ops/Add}

; fn add(a: u64, b: u64) -> u64 { Add::add(a, b) }
{_define_fn (add (:u64 a) (:u64 b) (:u64 (Add/add a b))}

{_define_fn ((:<→ u64 u64> inc)  x) (add x 1)}

{_define_fn (main) (:() (block
    (inc 10)))}

{fn ((:<→ u32 u32 u32> add) a b) ...}

{fn (f a) (g a)}

{fn (g a) (:u32 )(+ 1 a))}

{fn (main) (f 1)}
