;(--def-const if_ (lambda (p c e) (cond (p c) (else e))))

(--def-const minus (lambda (a b) (- a b)))

;(--def-const or (lambda (a b)
;    (cond (a true)
;          (b true)
;          (else false))))
;
;(--def-const gteq (lambda (a b) (or (> a b) (= a b))))

;(--def-const max (lambda (a b)
;    (if_ (gteq a b)
;         a
;         b)))

(--def-const (:<→ ()> main) (lambda () (println! "{}" (minus 3 4))))
