(def-macro-rules if ()
    ((predicate consequence otherwise)
     (cond (predicate consequence) (else otherwise))))

(def-const max (lambda ) (a b) (if (> a b) a b))

(def-const main (lambda ()
    (println! "{:?}" (max 14 (- 13 42)))))
